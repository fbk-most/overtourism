# SPDX-License-Identifier: Apache-2.0
"""Staging area for `runner.py` extensions that are candidates for future library merge.

Everything in this package is judged against one test: would a non-overtourism
`dt_model` domain plausibly need it too? Domain/example-family-specific code
(the overtourism examples' shared field math, output type, and presentation
metadata) lives in `overtourism.model.common` instead — see `overtourism/BACKEND_DESIGN.md`.
"""
