# SPDX-License-Identifier: Apache-2.0
"""Layer 5 REST API over the overtourism computation backends (Layer 3).

Depends only on `overtourism.model.*` — never on `overtourism.dt_studio.*`.
See `overtourism/BACKEND_DESIGN.md` for the layer rationale.
"""
